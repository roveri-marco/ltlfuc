MODULE main

VAR a : boolean;
    b : boolean;

#define _last !(X TRUE);

LTLSPEC NAME P1 := G(a -> X b);
LTLSPEC NAME P2 := G(a -> X !b);
LTLSPEC NAME P3 := F a;
LTLSPEC NAME P4 := G(b -> X !a);
LTLSPEC NAME P5 := G(b -> X a);
LTLSPEC NAME P6 := F b;
